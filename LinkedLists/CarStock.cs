using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.AccessControl;
using System.Text;
using System.Threading.Tasks;

namespace LinkedLists
{ /*
    class CarType
    {
        public string Name;
        public long Price;
        public string Color;
        public long Year;
        public long ID;

        public CarType next;
    }

    class StockList
    {
        private int size;

        public StockList()
        {
            size = 0;
        }

        public int Count
        {
            get { return size; }
        }

        public CarType head;

        public int Add(CarType new_car)
        {
            CarType sample = new CarType();
            sample = new_car;
            sample.next = head;
            head = sample;
            return size++;
        }

        public CarType Retrieve(int Position)
        {
            CarType current = head;
            for (int i = 0; i < Position && current != null; i++)
                current = current.next;
            return current;
        }

    }  */
}
